# Core ML
torch>=2.1
numpy>=1.24,<2.0          # music21/oemer stack is happier on numpy<2
tqdm>=4.66

# Symbolic music IO (MusicXML / MIDI parsing + writing fingerings back)
music21>=9.1

# OMR (image -> MusicXML). homr is the primary engine.
# Install homr separately if you only need symbolic input:
#   pip install homr
# oemer is kept as a fallback only.
homr>=0.6.0 ; platform_system != "Windows"
oemer>=0.1.7

# Optional: nicer plots / debugging
matplotlib>=3.7
